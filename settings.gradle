pluginManagement {
    repositories {
        // źródła, skąd pobierane są wtyczki (AGP/Kotlin)
        google()
        mavenCentral()
        gradlePluginPortal()
    }

    // tu przypinamy KONKRETNE wersje wtyczek:
    plugins {
        id("com.android.application") version "8.5.2"
        id("org.jetbrains.kotlin.android") version "1.9.24"
    }
}

dependencyResolutionManagement {
    // opcja zalecana: repozytoria tylko w settings.gradle
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "QR Parser"
include(":app")
